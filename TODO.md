# TODO

Continue in this order. Run `npm run verify` after each item.

1. Real OAuth connections to the platforms: a release is recorded against the slot, never pushed
2. Per-platform analytics and comment inboxes
3. Multi-tier pricing and seat-based billing; the Stripe scaffold is left as one price
4. A Stripe billing portal action: /premium explains that the portal link arrives with every receipt
5. Realtime subscriptions, a public API, video editing, mobile apps and white-label review domains
